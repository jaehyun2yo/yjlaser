# Supabase Migrations (Archived)

These SQL migrations were used when the project accessed the database via the Supabase client SDK.

As of Session #12 (2026-03-20), the project has been fully migrated to **Prisma ORM**.
All database migrations are now managed by Prisma at `webhard-api/prisma/migrations/`.

These files are kept for historical reference only and are **not executed**.
