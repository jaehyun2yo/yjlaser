# Features List

프로젝트 주요 기능 · 후속 태스크의 배포 상태 요약.
상세 변경 내역은 [docs/changelog/CHANGELOG.md](./changelog/CHANGELOG.md) 참조.

| Feature                                       | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status                                   |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- |
| integration-bank-notification-tracking        | IBK 알림 트래커 앱이 `/api/v1/integration/bank-notifications`로 은행 알림 원문 계약 필드(`event_id`, `raw_title`, `raw_text`, `raw_big_text`, `posted_at`)를 업로드하고, 관리프로그램이 같은 endpoint에서 `new`/`fetched` 후보를 조회한다. 서버는 `event_id` unique 기준으로 중복 업로드를 새 row 없이 기존 row로 수렴시키며, Android 전용 `source_package`/`notification_key`/`raw_payload`가 없는 권장 최소 payload도 수락한다. 앱은 `device_id`를 `dev-*` 해시 형태로 보내고 Android notification key 원문은 payload에서 제외하며, 수집 로그에는 원문/계좌/입금자명을 남기지 않는다.                                                                                                                                                                                                                                                                                                                                                                                        | implemented (2026-07-07, local verified) |
| external-webhard-confirm-integration-access   | 외부웹하드 동기화 API key가 `file/register` 권한으로 `/files/presigned-url`에서 routing된 Google Drive 업체 폴더 `folderId`를 그대로 `/files/confirm`에 전달해도 파일 metadata를 등록할 수 있다. upload endpoint는 route guard와 service entrypoint에서 integration principal 허용과 `file/register` 권한을 함께 요구하며, 일반 company session은 계속 자기 업체 폴더만 confirm할 수 있다. 동기화 key seed/생성 스크립트도 `external_webhard_sync` + `file/register`/`event/write` 권한으로 맞춰 legacy `sync` + `read/write/sync` key 재생성 회귀를 막는다.                                                                                                                                                                                                                                                                                                                                                                                                                   | implemented (2026-06-30, local verified) |
| operational-contact-identity-read-model       | 운영 데이터 연동 v2의 event/order/failure read model을 Contact 중심으로 정렬했다. worker event envelope는 `contact_id`, `inquiry_number`, `work_number`를 받고, `JobEvent`/`JobFailure`는 이 값을 raw payload와 별도 컬럼으로 보존한다. order timeline의 `contact_id`는 Contact UUID이며 legacy numeric `Order.contactId`는 `legacy_order_contact_id`로만 유지한다. `OrdersService.updateOrderStatus`는 `Order.inquiryNumber`로 Contact UUID를 찾아 상태와 workNumber를 동기화한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | implemented (2026-06-24, local verified) |
| central-log-db-persistence                    | YJLaser 공통 중앙 로그 수집 API가 운영 런타임에서 `log_events` Prisma table에 이벤트를 저장한다. 테스트 환경과 `LOG_EVENT_PERSISTENCE=memory`는 기존 in-memory repository를 유지한다. 중복/충돌 판정은 원본 client/key 저장 없이 `client_id_hash + event_id` unique key와 server-calculated `payload_hash`로 처리하며, 채널별 보관 만료일을 `retention_expires_at`에 저장한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | implemented (2026-06-23, local verified) |
| ui-driven-e2e-qa-suite                        | QA 자동화 기준을 실제 브라우저 UI 조작 Playwright 테스트로 확장했다. 공개 페이지/로그인/아이디·비밀번호 찾기/업체등록/문의 첨부, 관리자 문의·업체·예약·작업자·웹하드 화면, 업체 대시보드 자기 문의 격리와 새로고침 갱신, 업체 주문·피드백·청구, 웹하드 폴더·검색·업로드·툴바·권한, 작업자 PIN 로그인·문의 카드·납품관리·증빙 사진 첨부, 모바일/태블릿 overflow smoke를 `pnpm test:e2e:ui`로 실행한다. Playwright는 Next.js와 NestJS API를 함께 띄운다. 최초 실행에서 드러난 관리자 통합관리 hydration, 업체 세션 로그인 stuck, 모바일 overflow, 웹하드 새 폴더 UI, 작업자 PIN 빠른 입력, 업체등록 사업자등록증 Drive 업로드 500을 수정했고, Chromium UI E2E 전체 27개가 통과한다.                                                                                                                                                                                                                                                                                              | implemented (2026-06-19, local verified) |
| public-homepage-v1-active-restore             | 공개 홈 화면 `/`를 기존 V1 배포본 기준으로 다시 전환했다. 현재 홈은 `HomePageV1Backup`을 렌더하고 `HeroBoxSection`, `BoxNetSection`, `ProcessSection`, `PortfolioSection`, `InquirySection` 흐름과 V1 전용 `HomeHeader` 내비게이션을 사용한다. Spring/Summer 에디토리얼 홈은 현재 활성 홈 화면에서 제외됐다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | implemented (2026-06-25, local verified) |
| contact-drive-file-migration                  | 포트폴리오 이미지는 R2를 유지하고, 공개 문의 첨부/도면/참고사진, 수정요청 첨부, Worker 납품증빙, 사업자등록증 신규 업로드는 Google Drive로 전환했다. 문의 관련 파일은 업체 폴더의 해당 문의 폴더를 `ensureInquiryFolder`로 보장한 뒤 Drive에 저장하고 `WebhardFile.storageProvider=GOOGLE_DRIVE`로 등록한다. 사업자등록증은 `/외부웹하드/관리자전용/{업체명}` Drive 폴더 아래 관리자 전용 파일로 저장한다. 기존 R2 참조는 읽기 호환을 유지하며, 삭제 전 검토용 `webhard-api/scripts/dry-run-non-portfolio-r2-delete.ts` dry-run 리포트로 후보를 산출한다.                                                                                                                                                                                                                                                                                                                                                                                                                      | implemented (2026-06-18, local verified) |
| homepage-springsummer-editorial-redesign      | 공개 홈 화면을 Spring/Summer design.md 레퍼런스에 맞춰 Sage Paper 배경, Plum Ink 텍스트, Paper White 카드, 4px radius, 얇은 Stone/Ash border, 배경 없는 고정 내비게이션, 대형 display typography, 프로젝트 가로 캐러셀, 모바일 전체 메뉴, 언더라인 뉴스레터 입력으로 재구성했다. 기존 YJ Laser 로고는 유지하며 홈 화면에서는 기존 공통 Header/Footer/FloatingButtons 대신 전용 에디토리얼 레이아웃을 사용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | superseded (2026-06-25, V1 restored)     |
| webhard-ops-faster-feedback                   | 웹하드 업로드 confirm, batch confirm, 파일 삭제, 파일 이동의 사용자 응답은 Google Drive/DB 성공 경계까지만 기다리고, 알림 생성·활동 로그·자동 문의 보조 폴더 조회 같은 후처리는 백그라운드로 처리한다. Drive 생성/이동/휴지통 처리와 DB 반영은 여전히 fail-closed로 유지하며, 후처리 실패는 sanitized warn 로그로 남긴다. 파일 batch delete는 authorized id `Set`으로 재필터해 대량 삭제의 불필요한 O(n²) 비용을 제거했다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | implemented (2026-06-11, pending deploy) |
| webhard-storage-trash-usage                   | 웹하드 좌측 하단 저장공간은 활성 파일 용량과 휴지통 파일 용량을 함께 반영한다. `GET /api/v1/storage`는 `active`, `trash`, `current=active+trash`, 각 percentage 필드를 반환하며, 휴지통 파일도 실제 Google Drive/R2 저장공간을 차지하므로 전체 사용량에 포함한다. UI는 총 사용량 진행바에서 활성 용량과 휴지통 용량을 구분해 표시하고, 휴지통 비우기/복구/삭제 후 기존 storage query invalidation으로 즉시 갱신된다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | implemented (2026-06-11, pending deploy) |
| webhard-drive-proof-batch-operations          | Google Drive upload proxy가 Drive PUT 성공 응답을 HMAC 서명한 `driveUploadProof`로 반환하고, upload confirm/batch-confirm은 유효한 proof가 있으면 confirm-time Drive metadata GET 없이 storage file id와 대상 parent digest를 검증한다. proof가 없는 구버전 client는 기존 Drive API 검증으로 fail-closed fallback한다. Drive-backed 파일 batch move/delete는 Google Drive multipart batch 요청으로 한 번에 처리하고, 업로드 proxy/confirm/move/trash 로그에 elapsedMs, 성공/실패 수, proof 사용 수를 sanitized 값으로 남긴다. 성능 E2E 기준 100 file batch move 177.58ms, 100 file batch delete 102.30ms를 재측정했다.                                                                                                                                                                                                                                                                                                                                                         | implemented (2026-06-11, pending deploy) |
| webhard-folder-delete-optimistic-path-scope   | 웹하드 파일 삭제/이동 hook 경로는 현재 폴더 React Query cache에서 대상 파일을 즉시 제거하고 실패 시 rollback한다. 폴더 batch delete는 전체 폴더 parent map을 읽지 않고 선택 폴더의 indexed `path` prefix 하위만 조회하며, `path=null` 레거시 선택 폴더만 parent BFS fallback을 사용한다. Drive-backed folder batch delete는 선택 Drive folder trash를 제한 병렬로 실행하고 실패/DB 불일치 repair 로그를 남긴다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | implemented (2026-06-11, pending deploy) |
| webhard-drive-batch-session-move-optimization | Google Drive 기반 웹하드 batch upload session 생성 시 같은 요청 안의 폴더 권한 확인, 외부웹하드 routing, Drive target 조회를 request-local cache로 공유하고, Drive file id를 `generateDriveIds(count)`로 bulk 발급해 각 resumable upload session에 주입한다. Drive-backed batch file move는 source folder의 `driveFolderId`를 한 번에 조회해 `moveDriveFile.fromParentStorageFolderId`로 넘겨 provider의 per-file Drive metadata 조회 fallback을 피한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | implemented (2026-06-11, pending deploy) |
| webhard-folder-tree-query-dedupe              | 웹하드 `FolderTree`의 children/ancestors 조회를 React Query `queryKeys.webhard.folders.children/ancestors`로 통일해 메인 목록, breadcrumb, 트리 확장이 같은 캐시와 진행 중 요청을 공유한다. 폴더 진입 시 ancestors 5회와 children 2회까지 중복되던 요청을 각각 1회로 줄여 진입 요청 수를 9회에서 4회로 낮췄다. Drive-backed 5파일 작업 추가 측정에서는 upload session 4576ms, Drive proxy PUT 1663ms, batch confirm 911ms, batch move 2846ms, batch delete 1075ms가 나와 남은 병목이 Drive session 생성과 Drive-backed move 경계임을 기록했다.                                                                                                                                                                                                                                                                                                                                                                                                                                 | implemented (2026-06-11, pending deploy) |
| webhard-folder-entry-prefetch-trim            | 웹하드 폴더 children 조회 직후 하위 폴더 5개의 파일 목록을 자동 prefetch하지 않는다. 파일 목록 선조회는 사용자가 폴더에 hover하거나 실제 진입할 때만 수행해 폴더 진입 시 현재 폴더 파일 목록 외의 불필요한 `/api/webhard/files` 병렬 요청을 줄인다. 폴더 업로드 완료 후 파일/폴더/뱃지 cache invalidation은 background로 처리해 후속 refetch가 완료 UX를 막지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | implemented (2026-06-11, pending deploy) |
| webhard-performance-measurement-optimization  | 웹하드 폴더 접속, 업로드, 이동, 삭제 흐름을 Chromium E2E와 NestJS 성능 테스트로 기준 측정한다. 목록성 `/files` API는 빠르지만 폴더 진입 시 folders/children/ancestors/files/badge-counts 요청이 병렬·중복 호출되어 체감 지연이 커지는 구조와, Google Drive 업로드 batch-complete 및 삭제/이동 Drive 경계가 주요 병목임을 기록했다. `batchConfirmUpload`는 Drive metadata 확인을 제한 병렬 처리하고 폴더 readiness 조회를 캐시하며, 업로드 UI는 성공 후 후속 캐시 갱신을 background invalidation으로 처리한다. 폴더 이동은 이미 조회한 target parent metadata를 재사용하고 Drive-backed 폴더가 아닌 경우 불필요한 Drive parent 조회를 생략한다.                                                                                                                                                                                                                                                                                                                                 | implemented (2026-06-11, pending deploy) |
| google-drive-webhard-security-performance     | Google Drive 웹하드는 DB metadata를 빠른 목록 source of truth로 유지하되, 생성/수정/삭제/다운로드/진단/백그라운드 검증은 Drive 성공 없이는 정상 성공 처리하지 않는 fail-closed 경계로 보강한다. 일반 `/folders`, `/files`, `/files/search`는 Drive API를 호출하지 않고 DB pagination/visibility filter만 사용하며, 관리자 `GET /api/v1/storage/webhard-consistency`는 `lastCheckedAt`, `quotaBackoffCount`, 최근 `storage_repair` 이벤트를 함께 반환한다. `POST /api/v1/storage/drive-change-webhook`은 Google Drive change notification을 token/channel로 검증해 reconciliation queue에 넣고, 백그라운드 reconciliation은 최근 Drive-backed row를 점진 확인해 404/429/오류를 `sync_logs.metadata.auditKind='storage_repair'`로 남긴다.                                                                                                                                                                                                                                        | implemented (2026-06-10, pending deploy) |
| google-drive-webhard-e2e-qa                   | Google Drive 웹하드 전환 후 사용자 QA 항목을 Chromium E2E로 자동 검증한다. 업체 A/B 생성·승인, Drive provisioning ready, 업체 루트/템플릿 폴더, 업체별 화면/API 격리, 관리자 폴더 생성·이름변경·이동, 실제 Google Drive 프록시 업로드, 관리자/업체 다운로드, 파일 이름변경·이동, 검색, 새 파일 확인처리, 공유 링크 생성·다운로드·권한 차단, 단일 Drive 파일 ZIP, 파일 휴지통·복구, 업체 루트 삭제 차단 payload, 업체 삭제 후 웹하드 접근 차단, 30일 내 업체 복구, 최종 Drive API 정합성 진단 count 0을 검증한다. 공유 링크 DTO 불일치, share-links 권한 범위, API key-only 직접 파일 다운로드 차단, 업체 `company_id` 조작 차단, 공개 공유 다운로드 전용 stream endpoint, ZIP archiver CommonJS import 회귀도 함께 수정했다.                                                                                                                                                                                                                                                   | implemented (2026-06-04, pending deploy) |
| google-drive-webhard-consistency              | Google Drive 기반 웹하드 metadata 정합성을 DB와 서비스 경계에서 강제한다. `storageProvider=GOOGLE_DRIVE`인 폴더는 `driveFolderId`, 파일은 `driveFileId`가 필수이며 DB CHECK constraint와 업체별 active root partial unique index로 막는다. 기본 seed는 DB-only webhard fixture를 만들지 않고, legacy fixture를 명시적으로 켜는 경우에도 `R2` provider를 저장한다. 폴더/파일 생성 경로는 Drive 객체 생성/검증 실패 시 Google Drive row를 저장하지 않으며, 목록/트리/검색 API는 invalid Google Drive row를 정상 목록에서 제외한다. 운영 진단은 `GET /api/v1/storage/webhard-consistency`와 `scripts/audit-google-drive-webhard-consistency.ts`로 Drive id 누락, 업체 root 중복, Drive API 404 샘플을 확인한다.                                                                                                                                                                                                                                                                   | implemented (2026-06-02, pending deploy) |
| company-delete-webhard-trash                  | 관리자 웹하드에서 업체와 매칭된 루트 폴더를 직접 삭제하면 공통 모달로 매칭 폴더명과 업체명을 표시하고, 업체 상세 페이지 이동을 제공한다. 선택 삭제에 매칭 업체 루트 폴더와 일반 항목이 섞여 있으면 매칭 폴더를 삭제 대상에서 제외하고 나머지만 삭제할 수 있다. 업체 상세 페이지의 삭제 처리는 업체를 `status=deleted` 삭제 대기 상태로 전환하고, 기존 상태/웹하드 권한과 삭제 시각을 저장해 30일 이내 복구 가능하게 한다. 삭제 시 매칭 웹하드 루트 폴더와 하위 파일/폴더는 휴지통으로 이동하며, Google Drive id 누락 또는 Drive 404는 storage repair 로그로 남기고 DB 삭제는 계속 진행한다. 복구 시 업체 삭제 마커(`company:{id}`)가 있는 항목만 되돌리고, 동일한 Drive id 누락/Drive 404는 repair 로그 후 DB 복구를 계속한다.                                                                                                                                                                                                                                                 | implemented (2026-06-02, pending deploy) |
| google-drive-webhard-storage                  | 자체 웹하드 저장소의 신규 파일/폴더 저장 경로를 Google Shared Drive 기반으로 전환한다. PostgreSQL은 목록, 권한, 검색, 배지, 문의/작업자/납품 연동의 source of truth로 유지하고, Google Drive는 NestJS `StorageService` 뒤의 provider로만 접근한다. 업체 승인/활성화 시 업체 루트와 템플릿 폴더를 Drive에 provision하며, 실패 시 `drive_provisioning_status=failed`와 storage repair sync log를 남긴다. 브라우저 업로드는 Google resumable upload URL로 직접 전송하고 confirm 단계에서 Drive file id를 검증한다. 다운로드/ZIP/share/worker 파일 다운로드는 R2 presigned URL 또는 Drive stream proxy를 provider별로 분기한다. 개발 초기화는 기존 웹하드/문의/작업 데이터를 삭제하고 업체/계정은 보존하는 `scripts/reset-webhard-for-google-drive.ts`로 수행한다.                                                                                                                                                                                                                 | implemented (2026-05-29, pending deploy) |
| r3f-packaging-hero-test                       | `/test` 페이지에 Next.js App Router + React Three Fiber 기반 패키지 히어로 프로토타입을 추가한다. 공통 헤더/푸터/카피 없이 화면 중앙의 Canvas만 렌더하고, 스크롤 progress 0→1에 따라 첨부 GLB 기반 C형 제품 박스가 닫힌 형태에서 전개도 형태로 펼쳐진다. 박스 GLB는 제품 라벨, 바코드, 접힘선, 잠금 슬롯, tuck tab, side dust flap과 8개 Blender animation clip을 포함한다. R3F는 GLB animation을 `AnimationMixer.setTime()`으로 scrub하고, root Y축 360도 회전과 최종 화면축 정렬을 함께 적용한다. material clone에는 kraft 판지 색, grain/bump texture, 진한 접힘선 색을 적용하고 Canvas shadow, contact shadow, directional/hemisphere/fill light로 실제 박스 질감을 보강한다. 진행값은 ref로 전달해 useFrame에서 읽고, `prefers-reduced-motion`에서는 전개 자체를 유지하되 보간만 줄인다. Canvas는 desktop/mobile 모두 전개 완료까지 sticky 중앙 상태를 유지하며, 전개 스크롤 구간을 늘려 휠 입력 대비 progress가 완만하게 진행된다.                                       | prototype (2026-05-28, local only)       |
| e2e-admin-api-warning-cleanup                 | E2E 중 반복되던 admin API 경고를 줄인다. 알림 조회는 `userId` 없는 admin 전체 조회를 허용하고, active session 및 webhard folder config/template 호출은 API key principal이 아니라 session cookie 경로를 사용한다. sessions heartbeat/delete는 admin/company가 자기 세션만 갱신·삭제하며, folder template/config는 NestJS에서도 관리자 전용으로 고정한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | implemented (2026-05-27, pending deploy) |
| full-remediation-security-performance-gates   | 보안/성능/디자인/유지보수 전수 개선 GOAL의 핵심 회귀 게이트. forged session/API key principal/mutation side effect/worker ACL/secret fallback, 웹하드 pagination/direct-to-R2 upload/latest drawing, a11y/focus, design static gate, 변경 라인 기준 import/queryKey/any static gate를 테스트로 고정한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | implemented (2026-05-25, pending deploy) |
| webhard-normal-folder-infinite-loading        | 일반 웹하드 폴더 파일 목록이 최초 50개를 조회하고 `hasMore`가 있으면 같은 React Query cache에 다음 page를 append한다. 75개 이상 파일 폴더는 `VirtualizedFileList`를 통해 추가 page를 불러오며, query key는 page를 제외한 folder/search/sort scope를 유지한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | implemented (2026-05-25, pending deploy) |
| webhard-folder-upload-direct-r2               | 폴더 업로드는 Server Action에서 파일 바이트를 `arrayBuffer`/`Buffer`로 읽지 않는다. 폴더 구조만 서버 액션으로 생성한 뒤 브라우저가 presigned URL batch를 받아 R2로 직접 PUT하고 metadata batch-complete만 서버에 확정한다. folderId 그룹은 제한된 병렬 실행으로 처리하며, 파일명 매칭은 Map 기반 O(n)으로 동작하고 presign/metadata 요청에도 abort signal을 전달한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | implemented (2026-05-25, pending deploy) |
| chatbot-system-spec                           | 공개 `챗봇 상담` 버튼을 실제 상담 시스템으로 확장하기 위한 구현 준비 문서. 공개 방문자 FAQ/견적 준비/웹하드 안내/상담원 인계 흐름, public/company/admin role별 접근 범위, Next.js↔NestJS API 경계, 세션/메시지/인계 데이터 모델 초안, LLM/RAG source visibility, prompt injection 방어, 개인정보/토큰/파일 URL 노출 금지, 운영 지표와 테스트 계약을 정의한다. — [spec](specs/features/chatbot-system.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | proposed (2026-05-22)                    |
| webhard-storage-usage-live                    | 웹하드 좌측 하단 저장 공간 사용량은 업로드/삭제/복원 및 파일 realtime 이벤트 후 React Query 캐시를 즉시 무효화해 active 화면에서 갱신된다. 저장 공간 표시 컴포넌트는 window focus refetch와 30초 polling fallback을 가진다. 관리자 전체 저장 공간은 특정 `companyId=0` 행이 아니라 모든 `company_storage.used_bytes` 합계를 사용하며, company_storage 조회 실패 시 전체 활성 `webhard_files` 크기 aggregate로 보정한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | implemented (2026-05-22, pending deploy) |
| worker-booking-schedule-menu                  | Worker 대시보드 헤더에 `메뉴` 드롭다운을 추가해 오늘부터 14일간의 방문 예약 일정을 확인할 수 있다. 예약 목록은 worker 세션을 검증하는 read-only `/api/worker/bookings` 경로에서 조회하며, 관리자 예약 API를 직접 노출하지 않는다. 드롭다운은 예약 일자별로 시간대, 업체명, 연결 문의명/문의번호, 상태를 표시하고 새로고침 버튼으로 즉시 재조회할 수 있다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | implemented (2026-05-22, pending deploy) |
| company-login-remember-auto                   | 업체/관리자 로그인 화면에 `아이디 저장`과 `자동로그인` 선택지를 제공한다. `아이디 저장`은 비밀번호나 토큰 없이 아이디 문자열만 브라우저 `localStorage`에 저장해 다음 방문 시 로그인 폼에 복원한다. `자동로그인`은 성공한 로그인 세션 쿠키의 만료 시간을 30일로 연장하며, 선택하지 않으면 기존 4시간 세션 정책을 유지한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | implemented (2026-05-22, pending deploy) |
| worker-urgent-timeline-audit                  | Worker/Admin 긴급 토글은 현재 세션 actor를 기준으로 `ContactStatusHistory`에 `urgent_toggle` 이력을 남긴다. 긴급 배치와 긴급 해제 모두 contacts 업데이트와 타임라인 기록을 같은 트랜잭션에서 처리하며, 타임라인에는 `긴급 처리 — 작업자명` 또는 `긴급 해제 — 작업자명`과 기록 시각이 표시된다. Worker 대시보드는 긴급 토글 성공 직후 열린 카드의 타임라인을 즉시 refetch한다. 기존에 `is_urgent=true`/`urgent_at`만 있고 `urgent_toggle` 이력이 없는 문의는 `urgent_at` 기준 긴급 처리 fallback 이력을 표시한다. 긴급 배치 시 기존 관리자 `contact_urgent` 알림은 유지하고, 긴급 해제는 신규 긴급 알림을 만들지 않는다.                                                                                                                                                                                                                                                                                                                                                        | implemented (2026-05-22, pending deploy) |
| worker-global-search-jump                     | Worker 대시보드 검색 입력은 현재 탭만 필터링하지 않고 사무실 작업, 현장 작업, 납품관리 대기/완료 건을 함께 검색한 결과 드롭다운을 표시한다. 결과는 아래로 열리는 애니메이션 목록으로 표시되며, `ArrowDown`/`ArrowUp`으로 선택하고 `Enter`로 이동할 수 있다. 결과 목록은 처음 12개만 렌더하고 드롭다운 스크롤 하단 접근 시 다음 묶음을 추가 렌더링한다. 사무실/현장 결과를 클릭하거나 Enter로 선택하면 해당 탭과 필터로 이동해 카드를 스크롤·강조하고, 납품 대기 결과는 `납품관리`, 납품 완료 결과는 `납품완료` 태그로 표시한다. 납품 완료 후보와 완료 탭 검색은 완료 목록 원본을 합친 뒤 대시보드 공통 검색식으로 파일명까지 다시 필터링한다. 완료 탭에서 highlight 대상이 누락되면 단건을 보강 조회하고 DOM 렌더 후 스크롤을 재시도한다. 납품 결과를 선택하면 `/worker/delivery?tab=pending` 또는 `/worker/delivery?tab=completed`로 이동해 대상 납품 카드를 brand ring과 왼쪽 bar로 강조한다.                                                                                | implemented (2026-05-22, pending deploy) |
| worker-expanded-card-shimmer-loading          | Worker 문의 카드 확장 타임라인과 납품 완료 카드 확장 패널은 좌우로 흐르는 shimmer 스켈레톤을 사용한다. 문의 카드 타임라인 스켈레톤은 전용 빠른 highlight와 은은한 pulse를 적용해 로딩 중 반짝이며 움직이는 느낌이 더 분명하다. 납품 완료 카드 확장 시 타임라인과 납품 증빙 이미지가 모두 준비되기 전까지 전체 스켈레톤을 유지하고, 모든 요소가 준비되면 실제 콘텐츠를 함께 표시한다. 납품 완료 목록 초기 로딩도 카드형 shimmer 스켈레톤으로 표시한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | implemented (2026-05-22, pending deploy) |
| worker-contact-list-infinite-render           | Worker 대시보드의 사무실/현장 문의 카드 리스트는 최초 20개만 렌더하고, 스크롤 하단 접근 시 20개씩 추가 렌더링한다. 탭, 공정 필터, 검색어가 바뀌면 렌더 배치를 초기화하고, 새 문의 알림 클릭 대상 카드가 아직 렌더되지 않았으면 해당 위치까지 먼저 렌더한 뒤 스크롤한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | implemented (2026-05-21, pending deploy) |
| worker-timeline-file-row-download-ux          | Worker 타임라인 도면 파일 행은 별도 `다운로드` 텍스트 버튼을 표시하지 않는다. 파일 행 전체가 다운로드 컨트롤로 동작하고, 파일명 뒤에는 다운로드 아이콘 1개만 표시한다. 기존 blob 다운로드와 `문의번호 - 업체명 - 파일명` 저장명 보정 로직은 유지한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | implemented (2026-05-21, pending deploy) |
| worker-dashboard-live-clock                   | Worker 대시보드 상단 헤더 중앙에 현재 날짜와 시각을 표시한다. 날짜는 `26년 M월 D일 요일` 형식으로 위쪽에 큰 볼드 텍스트로, 시간은 아래쪽 보조 텍스트로 분리해 보여준다. 시각은 페이지를 열어둔 상태에서도 분 경계에 맞춰 자동 갱신하며, 작은 화면에서는 헤더 액션과 겹치지 않도록 숨긴다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | implemented (2026-05-21, pending deploy) |
| worker-login-failure-help-placement           | Worker 로그인 화면의 관리자 문의 안내는 최초 진입 시 숨기고, 이름/PIN 로그인 실패 후에만 표시한다. 실패 안내는 `이름과 PIN을 입력해주세요` 문구 바로 아래와 작업자 이름 입력 영역 위에 배치해 실패 메시지와 관리자 문의 안내를 함께 보여준다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | implemented (2026-05-21, pending deploy) |
| worker-new-contact-notification-read-state    | Worker 새 문의 알림은 `readAt` 읽음 상태를 storage에 보존한다. 카드 클릭, 카드 액션 클릭, 작업상태 변경, 알림 항목 클릭 시 해당 문의의 빨간 새 문의 UI를 제거한다. 알림창에서 항목을 클릭하면 드롭다운을 닫지 않고 대상 문의로 이동하며, 읽음 항목은 빨간 점 없이 밝은 회색 텍스트로 남는다. 읽음 처리 직후에는 위치를 유지하고, 새 문의 드롭다운을 닫을 때 미확인 알림을 위로, 확인된 알림을 아래로 재정렬한다. 읽은 지 3일이 지난 알림은 제거하지만 미확인 알림은 오래돼도 유지한다. `모두 확인`은 삭제가 아니라 읽음 처리이고, `비우기`만 목록을 제거한다. 새 문의 알림 목록은 처음 12개만 렌더하고 스크롤 하단 접근 시 다음 묶음을 추가 렌더링한다.                                                                                                                                                                                                                                                                                                                        | implemented (2026-05-21, pending deploy) |
| timeline-revision-download-filename-fix       | Worker/공통 타임라인의 도면 수정 파일 다운로드는 `/api/drawing-revisions/:revisionId/download`가 반환한 `문의번호 - 업체명 - 파일명`을 실제 저장명으로 사용한다. presigned R2 URL을 직접 열면 브라우저가 cross-origin `download` 속성을 무시할 수 있으므로 `DownloadButton` API 다운로드 경로는 파일을 blob으로 받은 뒤 object URL에 서버 반환 파일명을 적용한다. 기존 `[O]`/`[F]` prefix는 제거되고 현장번호만 있는 문의는 `F번호 - 업체명 - 파일명`으로 저장된다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | implemented (2026-05-21, pending deploy) |
| worker-memo-api-csrf-fix                      | Worker 로그인은 `erp-session`과 함께 `csrf-token`을 발급한다. 기존 worker 세션처럼 csrf cookie가 없는 경우에도 worker Server Action과 worker route proxy가 NestJS 상태 변경 요청을 보낼 때 upstream 요청용 matching `csrf-token` cookie/header를 생성해 메모/이슈 보고, 도면 업로드, 공정 변경 등 worker mutation이 `CSRF token missing` 403으로 막히지 않게 한다. Worker 메모/이슈 보고는 검증된 worker session의 이름으로 저장하고 클라이언트 body의 `workerName`은 신뢰하지 않는다. `worker_notes` 응답 내부 항목은 `created_by`/`created_at` snake_case 계약을 유지해 Worker 모달에 작업자명과 작성일이 정상 표시된다.                                                                                                                                                                                                                                                                                                                                                     | implemented (2026-05-21, pending deploy) |
| worker-drawing-upload-cookie-scope-fix        | Worker 도면 업로드/업로드 URL 발급 프록시는 `getErpWorkerSession()`으로 검증한 뒤 NestJS에 `erp-session`/`csrf-token`만 전달한다. 같은 브라우저에 stale `admin-session` 또는 `company-session`이 남아 있어도 NestJS worker guard가 잘못된 세션을 먼저 선택하지 않아 `Verified worker session required` 403으로 업로드가 막히지 않는다. Worker 파일 목록/다운로드 프록시도 같은 공통 header helper를 사용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | implemented (2026-05-21, pending deploy) |
| worker-card-download-webform-webhard-sync     | Worker 문의 카드 표시명은 화면에서만 `업체명 - 파일명`으로 정리하고, 업체명은 굵게, 파일명은 보통 두께로 표시한다. 다운로드명은 `문의번호 - 업체명 - 파일명`으로 한 번만 조립한다. 이미 route 또는 WebhardFile 단계에서 붙은 `[O]`/`[F]`, 전체 다운로드 prefix, 짧은 `O-001`/`F-001` prefix는 제거해 중복 다운로드명을 방지한다. 공개 문의 폼에서 업로드한 도면, 일반 첨부, 참고 사진은 Contact 생성 후 문의 폴더의 `WebhardFile`로 등록되어 자체웹하드 문의 폴더에서 조회된다. 실제 R2 key와 기존 WebhardFile 표시명은 재작성하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                     | implemented (2026-05-21, pending deploy) |
| worker-download-auth-fix                      | Worker actor mutation이 NestJS로 전달하는 Cookie 헤더를 `erp-session`/`csrf-token`으로 제한해, 같은 브라우저에 admin/company 쿠키가 남아 있어도 `process-stage`, 문의 분류, 작업 완료, 노트/긴급/단계완료 계열 요청이 검증된 worker session으로 처리된다. Worker와 관리자 문의 카드 다운로드 파일명은 `문의번호 - 업체명 - 파일명` 형식으로 통일하며 기존 `[O]`/`[F]` prefix 중복을 제거한다. 관리자 카드의 최신 DrawingRevision 다운로드 route도 contact metadata를 조회해 파일명에 문의번호와 업체명을 포함한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | implemented (2026-05-21, pending deploy) |
| public-design-review-round2                   | gstack 디자인 리뷰 2차 지적사항 반영. 홈 모바일 hero의 와이어프레임을 H1/CTA와 분리하고, 홈 dark scroll 구간의 과도한 높이와 낮은 정보 밀도를 줄였다. `ProcessSection`/`InquirySection`은 정적 정보 섹션으로 정리해 스크롤 보상을 앞당겼다. 홈 `대표 제작 유형` 카드는 동일 placeholder 대신 유형별 구조 도면 예시, 구조 설명, 용도 차이를 제공한다. `/portfolio` 0건 상태는 `전체 (0)` 필터를 숨기고 대표 제작 유형/제작 상담 CTA로 연결하며, `/notice` 0건 상태는 운영 안내와 제작 문의 CTA를 제공한다. `/about` 본문 폭과 `/contact` 모바일 stepper/card 밀도를 조정했다. `/admin`, `/company/dashboard`, `/webhard` 비인증 접근은 `/login?next=...`로 목적지를 전달해 로그인 화면에서 관리자/업체/웹하드 접근 맥락을 표시하고 실패 후에도 허용된 목적지 문맥을 유지한다. Worker 로그인에는 `작업자 이름` 고정 label과 PIN 실패/잠금/문의 help 영역을 추가했다.                                                                                                             | implemented (2026-05-20, pending deploy) |
| public-design-review-round1                   | gstack 디자인 리뷰 1차 지적사항 반영. 모바일 문의 폼을 가리던 작은 화면 경고 토스트를 제거하고, 공개 화면 모바일 플로팅 CTA는 단일 `상담` 버튼으로 접어 본문/빈 상태와 겹치지 않게 했다. 홈의 placeholder 카드는 실제 포트폴리오가 아니라 `대표 제작 유형`으로 표시하고 상담 CTA로 연결하며, `/portfolio` 빈 상태는 실제 작업 사례 준비 중 안내와 제작 상담 CTA를 제공한다. 홈의 긴 검은 스크롤 섹션과 전환 그라데이션 높이를 줄이고, 홈 CTA 연락처를 footer와 같은 `02-2264-8070`으로 통일했다. React Query Devtools와 react-grab/Claude Code 브라우저 스크립트는 명시 env가 있을 때만 개발 화면에 표시되거나 주입된다.                                                                                                                                                                                                                                                                                                                                                       | implemented (2026-05-20, pending deploy) |
| worker-auth-boundary-hardening                | Worker 인증 경계를 쿠키 존재 확인에서 검증된 `erp-session` + worker/admin actor 확인으로 보강했다. `/api/erp/session`은 이름/PIN을 NestJS `pin-login`에서 검증한 성공 응답으로만 httpOnly 세션을 발급하고, `/api/worker/**`, `/api/socket-auth`, worker-facing Server Action은 `getErpWorkerSession()` 또는 admin session 실패 시 backend 호출/token 발급 전에 401/권한 실패로 종료한다. Worker 파일/도면 프록시와 actor mutation client는 worker/admin actor 요청을 `X-API-Key`가 아니라 session cookie 경로로 NestJS에 전달하며, NestJS Files read endpoint와 Contacts worker mutation endpoint는 `erp-session` 허용 route에서만 worker session을 받고 API key 기반 worker actor 위조를 거부한다. Worker route guard와 React Query `enabled` 조건으로 인증 확인 전 데이터 조회를 막고, `ContactsGateway`의 검증 실패 `erp-session` fallback worker room join을 제거했다. Worker login의 PIN localStorage 자동 로그인 저장과 debug backend-health production 노출도 제거했다. | implemented (2026-05-19, pending deploy) |
| dashboard-webhard-security-audit-fixes        | 관리자/업체 대시보드와 웹하드 전수조사 후속 수정. contacts/bookings Next.js 라우트에 admin/company 세션 및 업체 소유권 검증을 추가하고, company session의 임의 `companyName` query를 세션 업체명으로 고정한다. 예약 생성/수정/취소는 연결 문의 `contactId`의 업체 소유권도 확인한다. 도면 revision standalone 다운로드는 업체 소유와 공개 여부 확인 후 presigned URL을 발급하고 visibility 변경은 admin 전용이다. 업체 웹하드는 접근 정보 조회 실패 시 fail closed로 막고, 백업 프록시는 admin cookie와 NestJS `Set-Cookie`를 보존한다. 예약 정원 비교는 backend `maxCapacity`를 사용하며 예약 조회/예약 가능 조회 실패는 빈 데이터로 흡수하지 않는다. 웹하드 className 오타와 Jest `.worktrees` 탐색 문제도 함께 수정했다.                                                                                                                                                                                                                                                    | implemented (2026-05-18, pending deploy) |
| company-dashboard-action-buttons              | 업체 대시보드 예약 일정 카드와 문의 진행상황 카드의 `예약변경`, `예약취소` 버튼은 `웹하드`, `메모`와 같은 `CardActionButton` 기반 카드 액션 스타일을 사용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | implemented (2026-05-18, pending deploy) |
| company-dashboard-delivery-proof-image        | 업체 대시보드 문의 카드의 납품 증빙 사진은 raw R2 URL을 직접 렌더링하지 않고 `/api/contacts/:id/delivery-proof` 권한 검증을 거친 presigned URL로 표시한다. 납품 증빙 사진은 해당 문의 폴더에 `납품완료_YYYYMMDD_HHmmss.ext` 웹하드 파일로 저장되는 backend 계약을 유지한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | implemented (2026-05-18, pending deploy) |
| company-dashboard-inquiry-title-display       | 업체 대시보드 예약 일정은 `/bookings` 응답의 `contact_id` 연결 문의 요약(`contacts`)으로 문의명을 표시한다. 문의 진행상황과 예약 일정 카드 제목은 저장 데이터를 변경하지 않고 화면 표시 시에만 앞쪽 업체명 접두사를 제거해 패키지명 중심으로 보여준다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | implemented (2026-05-18, pending deploy) |
| contact-submit-loading-state                  | 공개 문의 폼 마지막 단계에서 `문의하기` 클릭 후 제출이 진행되는 동안 버튼을 비활성화하고 `전송중...` 라벨과 회전 spinner를 표시한다. 버튼은 디자인 시스템 `Button` 컴포넌트를 사용하고 `aria-busy`를 제공해 중복 제출을 줄인다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | implemented (2026-05-18, pending deploy) |
| visit-booking-submit-payload-fix              | 공개 문의 제출 후 방문 예약 자동 생성 시 Next.js `submitContact`가 문의 저장용 snake_case 필드를 NestJS `/bookings`로 그대로 보내 `CreateBookingDto` 검증에서 400이 나던 문제를 수정했다. 예약 생성 body는 `visitDate`, `visitTimeSlot`, `companyName`, `contactId`, `createdBy` camelCase 계약을 사용하고, `serverCreateBooking` 입력 타입을 좁혀 회귀를 막는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | implemented (2026-05-18, pending deploy) |
| company-account-recovery-email                | 거래처 아이디 찾기는 화면에 전체/마스킹 아이디를 표시하지 않고 등록 이메일로 아이디 안내 메일을 예약한다. Next.js `/api/auth/find-id`와 `/api/auth/find-password`는 계정 복구 전용 IP/fingerprint rate limit을 거친 뒤 `X-Account-Recovery-Key`로 보호된 NestJS auth endpoint에 위임한다. post-lookup 계정/email 발송 제한과 메일 실패는 generic 200 응답을 유지해 계정 존재 여부를 노출하지 않으며, reset link는 `/reset-password#token=...` fragment 방식으로 발급하고 reset password 화면은 mount 직후 fragment 또는 legacy query token을 memory state에 보관한 뒤 주소창 token을 제거한다. Sentry URL/query_string/breadcrumb redaction과 계정 복구 메일 실패 로그 분류 코드로 token/email 원문 로그 노출을 줄인다.                                                                                                                                                                                                                                                        | implemented (2026-05-18, pending deploy) |
| admin-dashboard-notifications                 | 관리자 알림 시스템을 웹하드/통합관리/작업관리 카테고리로 분류한다. 웹하드 파일 업로드, 방문 예약 생성/변경/취소, 업체 승인 필요/승인/상태 변경, 신규 문의, 작업자 메모/이슈/요청, 긴급 지정이 admin notification으로 저장된다. 알림 API는 `category` 필터와 unread summary를 제공하고, 상단 알림 센터와 관리자 대시보드 알림 패널에서 카테고리별로 확인할 수 있다. 관리자 대시보드는 컴팩트한 카드/3열 정보 영역으로 조정하고 바로가기 섹션을 제거했다. 통합관리는 업체관리 중심으로 열리며 기존 통합관리 대시보드, 재고관리, 납품관리, 현장작업 admin 화면은 제거했다.                                                                                                                                                                                                                                                                                                                                                                                                        | implemented (2026-05-15, pending deploy) |
| public-homepage-v1-restore                    | 공개 홈 화면 `/`를 보존되어 있던 v1 구성으로 되돌렸다. 현재 홈은 `HomePageV1Backup`을 렌더하며 기존 `HeroBoxSection`, `BoxNetSection`, `ProcessSection`, `PortfolioSection`, `InquirySection` 흐름을 사용한다. build-time NestJS API 호출 회피 정책은 유지하고, 포트폴리오 영역은 빈 `portfolioItems`를 전달해 기존 placeholder fallback으로 표시한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | implemented (2026-05-15, pending deploy) |
| worker-pin-brute-force-rate-limit             | Worker PIN 로그인은 동일 IP의 최근 5분 `login_failed` access log가 5회 이상이면 worker 조회 전에 차단한다. 차단 응답은 `reason='rate_limited'`와 `retry_after_seconds`를 포함하고, 실패 로그 metadata에는 reason, 실패 횟수, retry 시간을 남긴다. 잘못된 PIN과 IP 차단 응답도 각각 `invalid_credentials`, `ip_blocked` reason을 제공한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | deployed (2026-05-14, needs QA)          |
| webhard-classify-failed-notification-dedupe   | 외부웹하드 AutoContact가 미분류 문의를 만들 때 `webhard_classify_failed` admin notification을 생성하되, 같은 `folderPath`에 대해 최근 1시간 안에 같은 알림이 있으면 중복 생성하지 않는다. `new_contact` 알림은 유지하고 classify failed 알림만 dedupe한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | deployed (2026-05-14, needs QA)          |
| worker-new-contact-realtime-notifications     | Worker 대시보드 `납품관리` 왼쪽에 새 문의 알림 드롭다운을 제공한다. `contact:created` 수신 즉시 알림에 추가하고 전체 process board 쿼리를 즉시 무효화해 새로고침 없이 반영한다. 알림 목록은 브라우저 storage에 보존되어 새로고침 후에도 유지되고, 항목 클릭 시 사무실/현장 탭과 하위 필터를 선택한 뒤 해당 문의 카드로 스크롤하면서 확인한 알림을 제거한다. 이동한 문의 카드는 잠시 `border-brand`/`ring-brand`/`bg-brand-light`로 강조 표시한다. `모두 확인`/`비우기`로 전체 목록을 초기화한다. 알림에 남아 있는 문의 카드는 미분류/접수 카드와 동일한 `bg-error` 빨간점을 표시하며, 알림 드롭다운은 넓은 상하좌우 여백을 사용한다. 외부웹하드 AutoContact 신규 생성도 admin/worker `contact:created` 이벤트를 발행하고, batch 자동문의 생성은 폴더/업체 조회 캐시 + 제한 병렬 처리로 빠르게 시작한다.                                                                                                                                                                        | implemented (2026-05-13, pending deploy) |
| worker-inquiry-download-latest-number         | Worker 문의 카드 다운로드 버튼은 현재 공정 기준이 아니라 마지막으로 업로드된 DrawingRevision을 최신 도면으로 다운로드한다. 다운로드/WebhardFile 파일명 prefix는 현장작업번호(`workNumber`)가 있으면 공정 단계와 무관하게 F 번호를 우선하고, 없을 때만 사무실작업번호(`inquiryNumber`)를 사용한다. 기존 `[O]`/`[F]` prefix는 제거 후 선택된 작업번호 하나만 다시 붙여 중복 prefix를 방지한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | implemented (2026-05-12, pending deploy) |
| integration-nesting-tasks                     | 레이저네스팅프로그램이 서버 큐에서 작업을 가져가고 결과를 보고할 수 있는 `GET /integration/nesting-tasks/pending`, `PATCH /integration/nesting-tasks/:taskId/status`, `POST /integration/nesting-tasks/:taskId/result` API. pending 작업은 priority/createdAt 순으로 조회하고, 상태 전이는 `pending -> in_progress -> completed/failed` 계약을 검증한다. 결과는 총 시트 수, 평균 사용률, 미배치 수를 `nesting_tasks`에 저장한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | implemented (2026-05-18, pending deploy) |
| integration-laser-completions                 | 레이저네스팅프로그램이 DXF 파일명에서 추출한 `workNumber` 목록으로 레이저 전용 문의를 완료 처리하는 `POST /integration/laser-completions` API. 항목별 `completed`/`already_completed`/`not_found`/`not_laser_only`/`failed` 결과를 반환하고, 완료 항목은 기존 `completeLaserOnlyContact` 정책(status completed, processStage null, timeline, socket/event emit, 문의 폴더 완료 이동)을 재사용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | implemented (2026-05-12, pending deploy) |
| webhard-folder-policy-unify                   | 업체 루트 하위 중간 `문의/` 폴더 도입 + Contact 생성 5 경로의 `ensureInquiryFolder` 훅 통합 + `registerFilesToWebhard` 레거시 제거                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | shipped (task 20)                        |
| webhard-inquiry-folder-gap-fix                | 미분류 문의 폴더 생성 + 공개폼 mismatch 가드 완화 + 외부웹하드 가상 업체 fallback 2단계화 + 실패 reason_code 진단                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | shipped (task 21)                        |
| contact-webhard-navigate                      | `relocateContactFiles` company 탐색 3단계 fallback 통일 (LGU+ 가상업체 도면 자동 이동 복구) + Admin · Worker 문의카드 우클릭 메뉴 "웹하드에서 열기" (Contact DTO `webhard_file_id` + 파일 하이라이트)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | shipped (task 22)                        |
| qa-contact-worker-v1                          | QA 제보 6종 일괄 해소: 공개 폼 도면 업로드 확장자 단일 상수화(`.ai` 허용) + 방문 예약 슬롯 UX·admin 승인/취소/수정 + Worker 접수 Contact 공정 시작 전 자동 분류·정보 보기 모달 + 사무실→현장 전환 silent fail 제거(422 throw) + 외부웹하드 자동 Contact 업체 대시보드 노출 복구 + Contact ↔ WebhardFolder 훅 `ContactFolderSyncService` 단일화                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | shipped (task 23)                        |
| external-sync-company-folder                  | 외부웹하드 동기화 → 가입 업체 폴더 통합. `matchCompanyInfo` 3단계 매칭 (0차 alias / 1·2차 insensitive equals / 3차 정규화 후보 자동 pending 등록) + `CompanyFolderAlias` 모델 + admin 승인 endpoint·UI (`/admin/integration/folder-aliases`) + `relocateAfterAliasApproved` cascadeBackfill 훅                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | shipped (task 24, 2026-04-27)            |
| webhard-visibility-and-external-inquiry-fix   | 웹하드 가시성 회복 + 외부 폴더명 alias 매뉴얼 매핑 + 미가입 업체 외부 sync 시 문의 폴더 자동화 회귀 가드. `FilesService` 3 진입점 폴더 `companyId` 상속 + 백필 마이그레이션 (Bug 1) + `POST /api/v1/companies/folder-aliases` admin 매뉴얼 매핑 endpoint (Bug 2) + `companyVisibilityFilter` helper 로 외부웹하드 root + 하위 일괄 차단 (Bug 3) — [spec](specs/features/webhard-visibility-and-external-inquiry-fix.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | shipped (task 25, 2026-04-28)            |
| external-folder-migration                     | 외부웹하드 폴더 통째 이전 + 신규 동기화 routing + admin UI 통합. alias 승인 시 `migrateExternalFolderTreeToCompany` 가 폴더 트리 자체를 가입 업체로 이전 + 빈 외부 폴더 cascade soft delete. 신규 동기화는 `getUploadPresignedUrl` 서버 측 routing 으로 처음부터 업체 경로 PUT (응답 `folderId`/`redirected` 옵셔널 추가). admin UI 는 `/admin/integration/companies` 로 통합 (`<FolderMappingSection>` 4 패널 + 매뉴얼 매핑 폼 + 미매칭 폴더 목록). 미분류 Contact 강제 통합 + R2 key 불변 — [spec backend](specs/features/external-folder-migration.md) · [spec UI](specs/features/admin-folder-mapping-ui.md)                                                                                                                                                                                                                                                                                                                                                               | shipped (task 26, 2026-04-29)            |
| external-batch-auto-contact-observability     | 외부웹하드 batch confirm routing 후 AutoContact 훅에 routed folder metadata 를 전달해 자동문의 skip 을 방지하고, presign/confirm/batch confirm/AutoContact detect·classify·create·folder sync 단계 서버 로그를 추가. 자동생성 Contact 는 매칭된 `companyId` 를 저장하고 미가입 업체는 null 유지.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | implemented (2026-05-08, pending deploy) |
| webhard-unclassified-file-relocate            | 웹하드 자동 생성 미분류 문의를 칼선의뢰/목형의뢰로 분류할 때 `relocateContactFiles` 가 `Contact.drawingFileUrl` 기반 원본 파일 경로를 이동 후보로 포함해, `DrawingRevision.webhardFileIds` 또는 문의번호 태그가 없는 기존 파일도 생성된 문의 폴더로 이동한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | implemented (2026-05-08, pending deploy) |
| backup-api-permission-scope                   | NestJS 백업 API에 `BackupAdminGuard`를 적용해 프론트 프록시에 의존하지 않고 admin session 또는 명시 백업 API key 스코프(`backup:read`, `backup:write`, `backup:execute`)만 허용한다. API key-only 무스코프 요청과 company session은 백업 설정 수정·수동 실행에서 거부된다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | implemented (2026-05-10, pending deploy) |
| webhard-monitoring-storage-scope              | `GET /activity-logs`에 `startDate`/`endDate` 필터와 invalid date 400 계약을 추가해 관리자 성능 화면의 24시간 활동 집계를 실제 시간 범위와 일치시킨다. 업체 `/storage/breakdown`은 자기 `companyId` 파일만 집계하고 `companyId=null` 관리자/null 파일은 제외한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | implemented (2026-05-10, pending deploy) |
| webhard-pipeline-observability                | 웹하드 업로드 라우팅 실패와 자동문의 skip reason을 `sync_logs.metadata.auditKind='webhard_pipeline'` 이벤트로 구조화한다. `GET /integration/sync-logs/pipeline-backlog` 및 관리자 통합 대시보드가 최근 실패/skip backlog를 조회하며, trace/context에는 presigned URL, token, raw API key, secret을 포함하지 않는다. — [spec](specs/features/webhard-pipeline-observability.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | implemented (2026-05-10, pending deploy) |
| webhard-lazy-folder-loading                   | `/folders` 기본 조회를 전체 트리에서 루트/직계 children lazy loading 계약으로 전환. breadcrumb는 `/folders/:id/ancestors`를 사용하고, 전체 구조가 필요한 캐시/호환 경로만 `/folders/tree` 또는 `includeAll=true`를 명시한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | implemented (2026-05-10, pending deploy) |
| webhard-badge-scope-invalidation              | 미다운로드 배지 집계의 backend 폴더 전파 범위를 `companyId`와 일치시키고, frontend React Query 배지 캐시 키를 `companyId`/`includeFolderCounts`별로 분리한다. 업로드·다운로드 확인·삭제·이동 후에는 scoped active badge cache를 refetch해 self/parent/root 배지가 서버 집계와 동기화된다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | implemented (2026-05-10, pending deploy) |
| external-webhard-candidate-bulk-counts        | 외부웹하드 미매칭 root 후보의 subtree file/contact count를 bulk groupBy로 계산하고, 빈 husk 후보도 depth=2 root의 직접 자식/직접 파일 존재 여부를 bulk 조회한다. root 수가 늘어도 후보 count 쿼리가 root별로 선형 증가하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | implemented (2026-05-10, pending deploy) |
| webhard-folder-path-prefix-update             | 폴더 rename/move 시 descendant materialized path를 재귀 순회 대신 transaction 내 SQL prefix 치환으로 일괄 갱신한다. slash-boundary 조건으로 sibling prefix 오염을 차단하고, 실패 시 cache invalidation/realtime event emit을 실행하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | implemented (2026-05-10, pending deploy) |
| webhard-frontend-stability-gates              | 웹하드 production code의 raw React Query key literal과 reload 기반 복구를 정적 테스트로 금지한다. 화면은 `VirtualizedFileList`를 단일 local virtual list로 유지하고, 업체명 조회 실패는 silent catch 대신 logger context를 남긴다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | implemented (2026-05-10, pending deploy) |
| webhard-main-query-hook-split                 | `WebhardMain.tsx`의 파일/새 파일/폴더 조회와 업로드 후 문의 연결 프롬프트를 전용 훅으로 분리한다. query key, drag payload, context menu selected-count, virtual list threshold는 `webhardMainContracts` 테스트로 고정한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | implemented (2026-05-10, pending deploy) |
| nestjs-server-client-domain-split             | `@/lib/api/nestjs-server-client`를 compatibility barrel로 유지하면서 공통 `nestjsFetch`와 도메인 client(`webhard`, `contacts`, `companies`, `operations`)를 분리한다. cookie/API key/auth/cache/retry 계약은 `core.client.ts`에 집중한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | implemented (2026-05-10, pending deploy) |
| webhard-backend-use-case-service-split        | `FoldersService`의 materialized path 계산/descendant prefix 갱신을 `FolderPathService`로, `FilesService`의 미다운로드 badge count 집계를 `BadgeCountsService`로 분리한다. 기존 controller/service public method는 유지하고 use-case 서비스 단위 테스트로 대량 서비스 회귀를 고정한다. — [spec](specs/features/webhard-backend-use-case-services.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | implemented (2026-05-10, pending deploy) |
| contact-form-submission-split                 | 공개 문의 `ContactForm`의 제출 payload 작성, reference→drawing 자동 승격, 방문/배송/납품처 최종 검증을 `contactSubmission` helper와 submit hook으로 분리한다. 단계별 section wrapper와 계약 테스트로 company info/file upload/visit booking/estimate method 순서를 고정한다. — [spec](specs/features/contact-form-submission-split.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | implemented (2026-05-10, pending deploy) |
| design-system-changed-file-static-gate        | 관리자/웹하드/스타일 우선 범위의 changed-file static gate를 추가해 수정·신규 파일의 `dark:`와 raw brand hex 재유입을 차단한다. 기존 전체 baseline debt는 `design-system.md`에 문서화하고, 현재 수정된 웹하드 파일은 토큰으로 치환했다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | implemented (2026-05-10, pending deploy) |
| webhard-qa-interaction-fixes                  | 웹하드 QA 피드백 반영. 파일/폴더 children 조회가 모두 준비될 때까지 메인 목록을 한 번에 렌더링하고, 파일 batch move는 출발/대상 폴더 모두에 realtime event를 emit한다. 업체 사용자는 삭제 액션이 노출되지 않으며 삭제 시도 경로는 "관리자에게 삭제 요청해주세요"로 안내한다. 폴더 생성은 관리자만 가능하고, 관리자 생성 child folder는 부모 `companyId`를 상속하며 기존 legacy `companyId=null` child도 업체 부모 아래에서 조회된다. 파일 더블클릭/다운로드는 다운로드만 수행하고 DXF 미리보기는 우클릭 메뉴의 별도 액션으로 분리했다. 파일 rename payload와 폴더 rename/move path SQL cast를 보정했다.                                                                                                                                                                                                                                                                                                                                                                        | implemented (2026-05-11, pending deploy) |
| webhard-qa-badge-rename-hover-followup        | 웹하드 QA 후속 반영. 업체 scoped 미다운로드 뱃지 전파가 legacy `companyId=null` bridge folder를 통과해 최상위 폴더까지 표시되도록 보정하고, 파일 hover preview 제거 + DXF preview modal 다운로드 버튼을 추가했다. 파일 rename은 `name`/`originalName`을 함께 갱신하며, 업체 폴더 이동은 프론트 drag/drop과 backend `PATCH /folders/:id/move` 모두에서 차단한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | implemented (2026-05-11, pending deploy) |
| webhard-folder-metadata-sort-badge-followup   | 웹하드 폴더 이동 후 active `badgeCounts` 캐시를 무효화해 이동 대상/상위 폴더 뱃지 합산이 즉시 다시 계산되게 했다. 폴더 목록 응답은 하위 파일 중 최신 업로드일/업로더 메타데이터를 포함하며, 메인 폴더 행은 업로드날짜/업로더 컬럼과 파일명/업로드날짜/업로더 정렬을 파일과 동일하게 지원한다. 한 자리 숫자 뱃지는 고정 원형 박스와 padding 0으로 중앙 정렬한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | implemented (2026-05-11, pending deploy) |
| webhard-session-expired-login-redirect        | 웹하드 화면을 장시간 열어둔 뒤 세션이 만료되어 파일/폴더 API가 401/419를 반환하면 빈 폴더 문구를 표시하지 않고 `/login`으로 리다이렉트한다. 인증 만료 오류는 `WebhardApiError`로 보존하고 React Query retry를 반복하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | implemented (2026-05-11, pending deploy) |
| webhard-click-selection-open-contract         | 웹하드 메인 목록 클릭 동작을 파일/폴더 모두 단일 클릭 선택, 더블클릭 실행으로 통일한다. 폴더 단일 클릭은 선택만 수행하고, 폴더 진입은 더블클릭으로만 실행한다. 파일은 기존 단일 클릭 선택/더블클릭 다운로드 계약을 유지한다. 이미 선택된 항목이 있을 때 다른 파일/폴더를 단일 클릭하면 기존 선택을 교체하지 않고 다중 선택에 추가한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | implemented (2026-05-11, pending deploy) |
| webhard-forwarded-cookie-bytestring-fix       | 서버 액션/웹하드 프록시가 NestJS API로 `Cookie` 헤더를 전달할 때 한글 등 non-ASCII 쿠키 값을 percent-encoding한다. 폴더 삭제 server action 경로에서 `Cannot convert argument to a ByteString...` 오류가 요청 전 헤더 생성 단계에서 발생하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | implemented (2026-05-11, pending deploy) |
| webhard-root-company-auto-contact             | `companyId=null`인 최상위 업체 폴더에 파일이 직접 들어와도 폴더명이 등록 업체명 또는 승인 alias와 매칭되면 AutoContact를 실행한다. 구조 루트(`외부웹하드`, 올리기/내리기 전용, 의뢰/완료 폴더)는 계속 자동 문의 대상에서 제외한다. 문의 생성 후 원본 도면 이동과 추가 도면 업로드 문의 폴더 배치는 기존 `ContactFolderSyncService`/`DrawingRevisionService` 계약을 유지한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | implemented (2026-05-11, pending deploy) |
| worker-inquiry-webhard-feedback               | Worker 추가 도면 업로드 후 타임라인/contact/process board/webhard 캐시를 함께 무효화하고, 추가 도면 WebhardFile 생성 시 폴더 `file:created` realtime 이벤트를 emit한다. 문의 폴더명은 번호 전용(`O`, `F`, `O_F`)으로 저장한다. 웹하드 목록/트리/검색/브레드크럼에는 `O /`, `/ F`, `O / F`로 표시하고, Worker 카드의 단일 문의번호는 불필요한 `/` 없이 번호만 표시한다. 생성 시간은 Worker 카드 오른쪽 버튼 그룹에서 다운로드 아이콘 왼쪽에 `26년 5월 12일 오전 10시 57분` 형식으로 표시하며, 기존 flex 헤더 구조에서 오른쪽 생성시간/아이콘을 가운데 제목 줄 중심선에 맞춘다. Worker 카드 저장 위치/웹하드 열기는 최신 파일의 현재 폴더를 우선한다. 업체 대시보드 `/contacts/by-company`도 최신 파일의 현재 폴더를 우선하고 최신 파일 연결이 없으면 `contactId` 연결 inquiry 폴더를 사용하며, 문의 카드에는 메모 왼쪽 `웹하드` 버튼을 제공해 해당 문의 폴더로 이동한다. 납품 완료 시 문의 폴더는 `문의/완료/` 하위로 이동한다.                                                 | implemented (2026-05-13, pending deploy) |
| delivery-proof-webhard-dashboard              | Worker 납품완료 사진 업로드 시 Contact의 `deliveryProofImage` 저장과 함께 해당 문의 폴더에 `납품완료_YYYYMMDD_HHmmss.ext` WebhardFile을 생성한다. 파일은 납품 완료 후 `문의/완료/` 하위 문의 폴더에 남고, 업체 대시보드 납품 완료 카드에서 증빙 사진을 표시한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | implemented (2026-05-12, pending deploy) |
| company-password-reset-link                   | 거래처 비밀번호 찾기 임시 비밀번호 발급 제거. Next.js `/api/auth/find-password` 는 NestJS reset-link request 로만 위임하고, NestJS 는 `password_reset_tokens` 에 hash-only 1회성 토큰을 저장한 뒤 메일 링크를 발송한다. `/reset-password` 에서 토큰 확인 후 새 bcrypt hash 로 갱신하며 기존 비밀번호는 confirm 전까지 유지. 토큰/임시 비밀번호/링크는 API 응답에 포함하지 않음.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | implemented (2026-05-06, pending deploy) |
| company-pending-login-message                 | 업체 등록 직후 관리자 승인 대기 계정(`is_approved=false`) 로그인 시 아이디/비밀번호 오류로 표시하지 않고 승인 대기 및 관리자 문의 안내를 표시. `status='pending'` 이 먼저 일반 실패 처리되던 로그인 분기 순서를 보정.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | implemented (2026-05-08, pending deploy) |
| worker-performance-optimization               | Worker 대시보드 프로덕션 성능 최적화. `nestjsFetch` ISR 캐시 옵션 (`cache` / `revalidate` / `tags`) + 읽기 Server Action 캐시 (목록/카운트/타임라인 30~60s) + `processBoard` 선택적 invalidation + 낙관적 업데이트·롤백 + `useContactTimeline` React Query 전환 + Socket.IO Worker 룸 추가                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | shipped (commit 2e683d95, 2026-04-09)    |
