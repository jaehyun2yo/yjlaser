# 개발 로드맵

> 프로젝트별 개발 목표와 진행상황을 체크박스로 관리한다.
> 대시보드에서 자동 파싱하여 완료율(%)을 계산한다.

<!--
포맷 규칙 (파싱 기준) — dashboard/parsers/roadmap-parser.js 참조

  1. 문서 제목: H1 헤딩 (파서가 무시)
  2. 프로젝트 섹션: H2 헤딩 (`##` + 공백 + 프로젝트명) = 섹션 시작
  3. 태스크 항목: `- [ ]` (미완료) / `- [x]` (완료) 체크박스
  4. 하위 태스크: 2칸 들여쓰기된 체크박스도 동일하게 카운트
  5. 체크 표시: 소문자 x, 대문자 X 모두 완료로 인식
  6. 일반 텍스트 줄(체크박스 아닌 줄)은 파싱 대상에서 제외
  7. 프로젝트명은 CLAUDE.md 활성 프로젝트 테이블의 이름과 일치시킬 것

  예시 계층:
    H2 프로젝트명
      체크(완료) 태스크 설명
      체크(미완료) 태스크 설명
        체크(완료) 하위 태스크 (들여쓰기)
-->

---

## yjlaser_website

- [x] 회사 홈페이지 기본 UI 구축 (Hero 3D 애니메이션, 포트폴리오, 블로그)
- [x] 거래처 회원가입/로그인 시스템 (bcrypt + httpOnly 세션 쿠키)
- [x] 관리자 대시보드 기본 레이아웃 (사이드바 + 라우팅)
- [x] NestJS 백엔드 API 서버 구축 (Prisma ORM, PostgreSQL)
- [x] Cloudflare R2 웹하드 파일 업로드/다운로드 API
- [x] 거래처 대시보드 (주문 현황, 웹하드 접근, 청구서 조회)
- [x] ERP Worker 작업관리 칸반보드 기본 기능
- [x] 문의 접수 폼 (도면 첨부, 이메일/Slack 알림)
- [x] SEO 최적화 (메타데이터, Open Graph, JSON-LD, sitemap)
- [x] Railway Docker 배포 (NestJS 백엔드)
- [x] Vercel 배포 (Next.js 프론트엔드)
- [ ] Worker 작업관리 시스템 고도화 (작업 배정 자동화, 알림)
- [ ] 거래처 대시보드 주문 실시간 알림 기능
- [ ] 관리자 ERP 칸반보드 드래그앤드롭 개선
- [ ] 웹하드 폴더 구조 트리뷰 UI 개선
- [ ] API Rate Limiting 강화 (Redis/Upstash 기반)
- [ ] 거래처 셀프 주문 접수 기능 (도면 업로드 -> 자동 견적)

---

## 외부웹하드동기화프로그램

- [x] Electron 기반 데스크톱 앱 기본 프레임 구축
- [x] LGU+ 웹하드 Playwright 기반 자동 로그인/파일 감지
- [x] 자체 웹하드(yjlaser.net) API 연동 업로드
- [x] SQLite WAL 모드 이벤트 영속화 (sync_events 테이블)
- [x] Dead Letter Queue 무한 재시도 방지 (영구 실패 마킹)
- [x] NAS 자동 업데이트 배포 파이프라인 (30분 주기 체크)
- [x] GitHub Release + NAS 듀얼 소스 자동 업데이터
- [x] 절전 모드 복구 (powerMonitor 기반 자동 재시작)
- [x] 커스텀 토스트 알림 시스템
- [x] Discord 웹훅 파일 감지 알림
- [x] 서킷 브레이커 상태 UI 노출 + 수동 리셋
- [x] 일별 회전 로그 파일 자동 영속화
- [ ] 동기화 충돌 시 사용자 알림 및 수동 해결 UI
- [ ] DLQ 항목 재시도 대시보드 추가
- [ ] 동기화 이벤트 로그 시각화 대시보드
- [ ] 단위 테스트 커버리지 80% 달성

---

## 유진레이저목형 관리프로그램

- [x] DXF 파일 자동 분류 기본 기능 (업체명/가격/날짜 파싱)
- [x] Popbill API 연동 청구서 FAX/MMS 발송
- [x] 이메일(SMTP) 청구서 발송
- [x] Excel 청구서 자동 생성 (openpyxl 템플릿)
- [x] SQLite 로컬 DB 기본 구조 (분류 이력, 발송 이력)
- [x] Supabase 클라우드 DB 듀얼 모드 (SQLite + PostgreSQL)
- [x] Multi-PC 마스터 선출 (단일 분류기 보장)
- [x] PyInstaller + Inno Setup 배포 파이프라인
- [x] 다크/라이트 테마 디자인 시스템
- [x] NAS/GitHub 자동 업데이트 기능 (version.json, 설치파일, 재시작 스크립트)
- [x] 청구서 리스트/매출 보고서 Excel 내보내기 기능
- [ ] Popbill FAILED 수동 재발송/관리자 알림 정책 정리
- [ ] DXF 파일 분류 정확도 향상 (패턴 매칭 규칙 추가)
- [ ] 코드 리팩토링 (Pydantic 모델 적용 확대)

---

## 레이저네스팅프로그램

- [x] ezdxf 기반 DXF 파일 파싱 (엔티티 필터링, 경계 사각형 감지)
- [x] 7가지 네스팅 알고리즘 구현 (Strip Packing 등)
- [x] PyQt5 GUI 기본 레이아웃 (캔버스, 도면 목록, 설정)
- [x] DXF 결과 출력 (R12 포맷)
- [x] Excel 배치 기록 자동 저장
- [x] 구아이(Guide) 선 감지 및 방향 자동 결정
- [x] PyInstaller Nasting.exe 단일 실행파일 빌드
- [x] 네스팅 결과 미리보기 확대/축소 및 팬 기능
- [ ] 현장 DXF 벤치마크 기반 알고리즘 단순화/품질 기준 확정
- [ ] 대형 DXF 파일 처리 시 메모리/시간 성능 예산 검증
- [ ] 현장 네트워크 폴더 감시 자동 로드 안정성 검증
- [ ] Shapely 2.0 기반 기하 연산 최적화
- [ ] 네스팅 히스토리 저장 및 비교 기능

---

## computeroff

- [x] FastAPI 서버 기본 구조 (REST API, SQLite DB)
- [x] Windows 에이전트 부팅/종료 이벤트 전송 (재시도 로직 포함)
- [x] 하트비트 기반 실시간 온라인/오프라인 상태 감지
- [x] 웹 대시보드 PC 목록/상태 표시
- [x] 일별 사용 요약 및 타임라인 (Chart.js)
- [x] 비밀번호 기반 세션 인증 + CSRF 보호
- [x] Windows 작업 스케줄러 연동 (에이전트 자동 시작)
- [x] Railway 클라우드 배포
- [x] Inno Setup 에이전트 설치파일 (Windows 7~11 지원)
- [ ] PC 부팅/종료 이력 주간 리포트 자동 발송
- [ ] 에이전트 미응답 시 관리자 알림 기능
- [ ] 대시보드 UI 반응형 레이아웃 개선
- [ ] PC 그룹별 관리 기능 (부서별 모니터링)
